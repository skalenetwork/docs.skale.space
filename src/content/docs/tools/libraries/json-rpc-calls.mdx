---
title: JSON-RPC Calls
description: Intro to JSON-RPC Calls
---
import { Tabs, TabItem } from '@astrojs/starlight/components';
import ToolLayout from "../../../../components/Tool/ToolLayout.astro";

<ToolLayout
    introParagraph="When some platforms or frameworks don't have any quality web3 library or SDK, there's always the possibility to make the blockchain calls directly to the JSON-RPC methods supported by the chain."
    toolName="JSON-RPC Calls"
    toolLink="https://docs.skale.network/technology/json-rpc-interface"
    toolDocsLink="https://docs.skale.network/technology/json-rpc-interface"
>

Some of the JSON-RPC methods supported by SKALE chains are:
- eth_getBalance
- eth_blockNumber
- eth_getTransactionCount
- eth_sendTransaction
- eth_call

## Implementation Example

**Get Balance example**
````typescript
import axios from 'axios';

const rpcEndpoint = 'YOUR SKALE RPC ENDPOINT';
const senderAddress = '0x...';


const requestData_balance = {
    jsonrpc: '2.0',
    method: 'eth_getBalance',
    params: [senderAddress, 'latest'],
    id: 1,
  };


async function JSON_RPC_CALL() {
    try {
        const response = await axios.post(rpcEndpoint,requestData_balance);
        return console.log(response.data);
      } catch (error) {
        console.error(error);
        return [[],[]];
      }
}

JSON_RPC_CALL();
````


</ToolLayout>