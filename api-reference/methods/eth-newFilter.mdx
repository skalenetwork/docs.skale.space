---
title: eth_newFilter
description: Creates a filter object based on filter options to notify when the state changes
openapi: api-reference/openapi.json POST /
---

<Api openapi="api-reference/openapi.json" path="/" method="post" example="eth_newFilter" />


Creates a filter object based on filter options to notify when the state changes (logs).

## Request

```json
{
  "jsonrpc": "2.0",
  "method": "eth_newFilter",
  "params": [
    {
      "fromBlock": "latest",
      "toBlock": "latest",
      "address": "0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb",
      "topics": []
    }
  ],
  "id": 1
}
```

### Parameters

1. **filter options** (`object`, required):
   - **fromBlock** (`string`, optional): Block number or `"latest"`, `"earliest"`, `"pending"`
   - **toBlock** (`string`, optional): Block number or `"latest"`, `"earliest"`, `"pending"`
   - **address** (`string` | `array`, optional): Contract address or array of addresses
   - **topics** (`array`, optional): Array of topic filters

## Response

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": "0x1"
}
```

### Response Types

- **result** (`string`): Filter ID as a hexadecimal string

## Example

<CodeGroup>

```bash Request
curl -X POST https://mainnet.skalenodes.com/v1/honorable-steel-rasalhague \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "method": "eth_newFilter",
    "params": [
      {
        "fromBlock": "latest",
        "toBlock": "latest",
        "address": "0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb"
      }
    ],
    "id": 1
  }'
```

```json Response
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": "0x1"
}
```

</CodeGroup>

## Notes

- Ignores logs from rotated blocks on Core/Full Sync nodes
- Archive nodes have access to all historical logs
- Use the filter ID with [eth_getFilterChanges](/api-reference/methods/eth-getFilterChanges) to get updates
- Available on all node types (with limitations on Core/Full Sync)

## Related Methods

- [eth_getFilterChanges](/api-reference/methods/eth-getFilterChanges) - Get filter changes
- [eth_getFilterLogs](/api-reference/methods/eth-getFilterLogs) - Get filter logs
- [eth_uninstallFilter](/api-reference/methods/eth-uninstallFilter) - Uninstall filter

